
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kinesia.app',
  appName: 'kinesia',
  webDir: 'dist',
  server: {
    url: '',
    cleartext: true
  },
  backgroundColor: "#000000"
};

export default config;
